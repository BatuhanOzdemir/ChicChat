// Placeholder seed runner wired in Step 0 so `npm run db:seed` exists and runs.
// The real idempotent seed (demo merchant, taxonomy, routing rules, demo order)
// is implemented in Step 2 — see CLAUDE.md / docs/SPEC.md §1 & §4.
console.log(
  "[db:seed] No seed data yet — implemented in Step 2 (taxonomy + demo order).",
);
